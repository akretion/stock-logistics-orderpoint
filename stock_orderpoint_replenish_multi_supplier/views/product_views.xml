<?xml version='1.0' encoding='utf-8'?>
<odoo>

    <!-- product.template inherit form view -->
    <record id="product_template" model="ir.ui.view">
        <field name="name">product.template.inherit.view.form</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="stock.product_template_form_view_procurement_button" />
        <field name="arch" type="xml">
            <button name="%(stock.action_product_replenish)d" position="after">
                <button string="Replenish by lot" type="object"
                    name="action_open_multi_supplier_replenish"
                    context="{'default_product_tmpl_id': id}"
                    groups="stock.group_stock_user"
                    invisible="type != 'consu' or not replenish_by_lot" />
            </button>
        </field>
    </record>
    <record id="product_product_form_inherit_replenish_by_lot" model="ir.ui.view">
        <field name="name">product.product.product.form.inherit.replenish.by.lot</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="stock.product_form_view_procurement_button" />
        <field name="arch" type="xml">
            <button name="%(stock.action_product_replenish)d" position="after">
                <button string="Replenish by lot" type="object"
                    name="action_open_multi_supplier_replenish"
                    context="{'default_product_tmpl_id': id}"
                    groups="stock.group_stock_user"
                    invisible="type != 'consu' or not replenish_by_lot" />
            </button>
        </field>
    </record>
    <record id="product_product" model="ir.ui.view">
        <field name="name">product.product.inherit.view.form</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="stock.product_product_view_form_easy_inherit_stock" />
        <field name="arch" type="xml">
            <button name="%(stock.action_product_replenish)d" position="after">
                <button string="Replenish by lot" type="object"
                    name="action_open_multi_supplier_replenish"
                    context="{'default_product_tmpl_id': id}"
                    groups="stock.group_stock_user"
                    invisible="type != 'consu' or not replenish_by_lot" />
            </button>
        </field>
    </record>
</odoo>
